TEST

var arr : int[]@{H} = [1,2,3,4,5];

START(x : int@{}) {
    var sum : int@{H} = 0;
    var i : int@{} = 0;

    ;;

    oblif 1
    then arr = [999,999,999,999,999,999,999,999,999,999,999];
    else skip;

    while i < arr.publength do {
        oblif i < arr.seclength
        then sum = sum + arr[i];
        else skip;
        i = i + 1;
    }
    print("Sum",sum);
}