{Bob}

hn ({Alice},"OFFER",int<m> offer) {
  obliv if offer >= 200
  then send({Alice},"ACCEPT",0);
  else send({Alice},"DECLINE",0);
}

hn ({Alice},"CONTRACT",string<m> contract) {
  print("Contract received",contract);
}
