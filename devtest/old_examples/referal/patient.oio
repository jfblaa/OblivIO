Patient

@{}

channel int@{Doctor} RESULT;

variable obliv int@{Clinic} time := ?0;

variable int@{Doctor} result := 100;
variable int@{Clinic} appointment := 0;

init {
  output RESULT(result);
}

int@{Clinic} APPOINTMENT(time) {
  skip;
} {
  appointment ?= time;
}
