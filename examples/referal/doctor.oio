Doctor

channel int@{Patient,Clinic} REFER;

variable obliv int@{Patient} result := ?0;

variable int@{Patient} patient_id := 123;

int@{Patient} RESULT(result) {
  skip;
} {
  obliv if !result > 50
  then output REFER(patient_id);
  else skip;
}
